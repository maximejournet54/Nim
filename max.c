#include <stdio.h>

int main ()
{
    int nlig, ncol, max, i, n;
    scanf("%d %d", &nlig, &ncol);
    for (i=2; i<=n; ++i)
    {
        if (nlig>ncol)
        max=nlig;
        else
        {
            max=ncol;
        }
        
    }
}